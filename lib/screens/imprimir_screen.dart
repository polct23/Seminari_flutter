import 'package:flutter/material.dart';

class ImprimirScreen extends StatelessWidget {
  const ImprimirScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Imprimir')),
    );
  }
}